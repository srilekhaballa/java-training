A final global variable can be assigned a value inside a constructor
Run time polymorphism works with interfaces as well
Reader and Writer should be used while working with binary data
Which of the following block can close/clean up the resources automatically - try with resources
The default implementation of equals method in the Object class compares the object references 
If the hashcode value of two objects is same then the equals should return true - 
Which of the following methods from the Object class should be overridden to display meaningful information when an object is used in a System.out.println - toString

We can use var declarations inside a lambda expression
We can declare a variable as a final string[] array and able to access that array index within a lambda expression